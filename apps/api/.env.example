# Local dev (con docker-compose):
DATABASE_URL="postgresql://allroads:allroads@localhost:5432/allroads?schema=public"

# Producción (Railway, Supabase, etc) — toma la URL que te da el proveedor:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME"

JWT_SECRET="cambia-esto-en-produccion-please"
JWT_EXPIRES_IN="7d"
PORT=4000
GOOGLE_MAPS_API_KEY=""

# CORS opcional (frontend en producción)
CORS_ORIGIN="https://tu-dominio.vercel.app"
